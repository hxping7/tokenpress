// 项目版本号单一来源。与根 package.json 的 "version" 字段保持一致。
// 修改版本时请同步更新：package.json(version) + 此处 APP_VERSION + git tag(vX.Y.Z)。
export const APP_NAME = 'TokenPress'
export const APP_VERSION = '1.0.0'
export const APP_VERSION_LABEL = `v${APP_VERSION}`
